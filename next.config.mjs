/** @type {import('next').NextConfig} */
const nextConfig = {
    // images: {
    //     domains: ['assets.aceternity.com',"images.unsplash.com"], // Add the hostname for external images
    //   },
};

export default nextConfig;
